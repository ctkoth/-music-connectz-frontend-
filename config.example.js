// Optional runtime config for Music ConnectZ
// Copy to `config.js` and replace the placeholder with your real Google Maps JS API key.
// Do NOT commit `config.js` with real secrets — keep it out of source control.

// Example:
// window.GOOGLE_MAPS_API_KEY = 'AIzaSy...YOUR_KEY_HERE...';

window.GOOGLE_MAPS_API_KEY = 'REPLACE_WITH_YOUR_KEY';
