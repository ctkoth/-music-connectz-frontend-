import React from 'react';

const KeyConnectZApp = () => (
  <div style={{ padding: 32 }}>
    <h1>⌨️ Key ConnectZ</h1>
    <p>Floating keyboard for mobile & desktop. Transcribe and translate any language, with custom backgrounds and voice-to-text as the main feature. Corey-style, everywhere.</p>
    {/* Future: Add language selection, background upload, and voice-to-text UI here */}
  </div>
);

export default KeyConnectZApp;
